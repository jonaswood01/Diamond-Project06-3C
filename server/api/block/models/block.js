'use strict';
//import { Sequelize, Model, DataTypes,  QueryTypes, sql } from '@sequelize/core';

/**
 * Lifecycle callbacks for the `block` model.
 */
// import { bModel} from './modelBlock.js';

// const sequelize = new Sequelize(process.env)

// try {
//      await sequelize.authenticate();
//      console.log('Connection has been established successfully.');
//    } catch (error) {
//      console.error('Unable to connect to the database:', error);
//    } 




 module.exports = {
//   // Before saving a value.
//   // Fired before an `insert` or `update` query.
//    beforeSave: async (model, attrs, options) => {

//     if(model != bModel)
//     {
//       console.log("model is being save");
//     }
//     else
//     {
//       console.log("model already on the system")
//     }



//    },

//   // After saving a value.
//   // Fired after an `insert` or `update` query.
//   afterSave: async (model, response, options) => {

//     if(model != bModel)
//     {
//       console.log("model is already save");
//     }
//     else
//     {
//       console.log("model already on the system")
//     }
//   },

//   // Before fetching a value.
//   // Fired before a `fetch` operation.
//   // beforeFetch: async (model, columns, options) => {},

//   // After fetching a value.
//   // Fired after a `fetch` operation.
//   // afterFetch: async (model, response, options) => {},

//   // Before fetching all values.
//   // Fired before a `fetchAll` operation.
//   // beforeFetchAll: async (model, columns, options) => {},

//   // After fetching all values.
//   // Fired after a `fetchAll` operation.
//   // afterFetchAll: async (model, response, options) => {},

//   // Before creating a value.
//   // Fired before an `insert` query.
//   beforeCreate: async (model, attrs, options) => {

//     if(model != bModel)
//     {
//       console.log("model is being created");
//     }
//     else
//     {
//       console.log("model already on the system")
//     }


//   },

//   // After creating a value.
//   // Fired after an `insert` query.
//   afterCreate: async (model, attrs, options) => {

//     if(model != bModel)
//     {
//       console.log("model was created");
//     }
//     else
//     {
//       console.log("model already on the system")
//     }


//   },

//   // Before updating a value.
//   // Fired before an `update` query.
//   beforeUpdate: async (model, attrs, options) => {

//     if(model == bModel)
//     {
//       console.log("do you want to updated this model?");
//     }
    
//   },

//   // After updating a value.
//   // Fired after an `update` query.
//   afterUpdate: async (model, attrs, options) => {

//     if(model == bModel)
//     {
//       console.log("model was updated?");
//     }

//   },

//   // Before destroying a value.
//   // Fired before a `delete` query.
//   beforeDestroy: async (model, attrs, options) => {

//     if(model == bModel)
//     {
//       console.log("do you want to delete this model");
//     }

//   },

//   // After destroying a value.
//   // Fired after a `delete` query.
//   // afterDestroy: async (model, attrs, options) => {}
};
